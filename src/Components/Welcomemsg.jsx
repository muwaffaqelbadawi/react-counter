import React from "react";
import "../Styles/welcomeStyle.css";

const Welcomemsg = () => {
  return <p className="welcome-msg">Welcome to counter app where you can use the button to count as much as you can!!</p>;
};

export default Welcomemsg;
